#include <stdio.h>
 
int main() {
 
    int A, B; //declarando as variáveis
scanf("%d %d", &A, &B); //recebenso valores do usuário
printf("SOMA = %d\n", A+B); // somando e imprimindo a soma
 
    return 0;
}