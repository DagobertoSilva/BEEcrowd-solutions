#include <stdio.h>

int main() {
 int a, b, X; // iniciando as variaveis
 scanf("%d %d", &a,&b); //recebendo as variáveis
 X = a+b; // fazendo a soma das variávei e colocando no X
 printf("X = %d", X); // imprimindo a soma
}
